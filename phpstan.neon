includes:
    - ./vendor/larastan/larastan/extension.neon

parameters:
    paths:
        - app/

    # Level 5 is a good baseline for "The Laravel Way" (Strictness: 0-9)
    level: 5

    # Optional: Ignore common things during migration if needed
    # ignoreErrors:
    #    - '#PHPDoc tag @var#'

    checkMissingIterableValueType: false
